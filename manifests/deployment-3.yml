apiVersion : apps/v1
kind: Deployment
metadata:
  name: "givemeahand-15e1"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "givemeahand-15e1"
  template:
    metadata:
      labels:
        app: "givemeahand-15e1"
    spec:
      containers:
        - name: "givemeahand-15e1"
          image: "givemeahandfront.azurecr.io/givemeahand"
          ports:
          - containerPort: 80