apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "givemehand-bea6"
    labels:
        app: "givemehand-bea6"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: givemehandback-givemehand-bea6.9ded8650132c448f9d7a.westeurope.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "givemehand-bea6"
                servicePort: 8080