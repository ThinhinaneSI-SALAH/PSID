apiVersion: v1
kind: Service
metadata:
    name: "givemeahand-0ac2"
    labels:
        app: "givemeahand-0ac2"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "givemeahand-0ac2"