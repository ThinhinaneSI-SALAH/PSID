apiVersion: v1
kind: Service
metadata:
    name: "givemehand-fae8"
    labels:
        app: "givemehand-fae8"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "givemehand-fae8"