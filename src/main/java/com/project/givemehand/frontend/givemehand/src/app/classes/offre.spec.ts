import { Offre } from './offre';

describe('Offre', () => {
  it('should create an instance', () => {
    expect(new Offre(undefined,undefined,undefined,undefined,undefined,undefined,undefined,undefined)).toBeTruthy();
  });
});
